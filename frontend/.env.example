# Copia este archivo como .env solo si vas a desplegar el frontend aparte
# del backend (ej: Vercel/Netlify). En desarrollo local NO hace falta:
# el proxy de Vite ya redirige /api al backend en localhost:4000.

# URL publica del backend en produccion (incluye /api al final)
VITE_API_URL=
